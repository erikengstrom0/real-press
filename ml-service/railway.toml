[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
dockerfileContext = "."

[deploy]
healthcheckPath = "/health"
# Increased timeout for PyTorch model loading on cold start
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
numReplicas = 1
